# 依赖（唯一的运行时依赖 yaml，由 npm install 还原，不入库）
node_modules/

# specgate 运行时产物（lint/plan/draft 在「用户工作目录」生成的输出，非源码）
# ⚠️ 用根级限定（/ 开头），避免误伤 templates/draft.prompt.md 等模板文件
review.md
contract.draft.yaml
/draft.prompt.md
specgate-plan/

# 日志
*.log

# 系统/编辑器
.DS_Store
Thumbs.db
# render-review.mjs 渲染 review.md 时产生的临时 HTML
docs/*.html
